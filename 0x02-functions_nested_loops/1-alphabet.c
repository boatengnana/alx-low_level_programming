#include "main.h"

/**
 * print_alphabet - prints all alphabets in upper case
 *
 * Return: always 0 (Success)
 */

void print_alphabet(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}
}
