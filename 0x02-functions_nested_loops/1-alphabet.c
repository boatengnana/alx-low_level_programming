#include "main.h"

/**
 * alphabet - prints all alphabets in upper case
 *
 * Return: always 0 (Success)
 */

void print_alphabet(void)
{
	char c;

	for (c = 'z'; c <= 'z'; c++)
	{
		_putchar(c);
	}
}
