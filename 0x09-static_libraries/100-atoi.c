#include "main.h"
#include <limits.h>

/**
 * _atoi - converts a string to an integer
 * @c: character
 *
 * Return: int
 */

int _atoi(char *c)
{
	int sign = 1;
	int num = 0;
	int i = 0;
	int prev_num = 0;

	return (sign * num);
}
