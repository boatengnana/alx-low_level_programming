#include <stdio.h>

/**
 * main - prints the name of a program
 * @argc: number of arguments at commandline runtime
 * @argv: the arguments themselves
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
